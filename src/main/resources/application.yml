# Application Properties
app:
  home-dir: D:\Temporary\CodingAppData

# Application Info
info:
  application:
    name: coding-test
    description: Sample App for Coding Test
    author: Shubham Shinde
    version: '@project.version@'

# Server Config
server:
  port: 8080

# Spring Security Config
logging:
  level:
    org:
      springframework: info

# Database Config
spring:
  profiles:
    active: mariadb
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
